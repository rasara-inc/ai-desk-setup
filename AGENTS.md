# 설치 에이전트 지침

이 폴더는 캐나다 한인 비개발자용 설치 키트다. 제품 개발 저장소가 아니다.
대화는 항상 한국어. 생성하는 폴더·파일명은 기본 영어, 본문은 한국어.
이 키트를 설치해 달라는 요청은 SETUP.md의 설치·개인화·검증까지 수행하라는 요청이다.

1. README.md → SETUP.md → manual/07-connections.md를 읽고 현재 OS·도구·기존 설정을 확인한다.
2. 이미 승인된 설치를 단계마다 재승인받지 않는다. 사용자 계정 로그인, OS 권한,
   실제 결제, 중요한 삭제·외부 발송처럼 사람이 필요한 단계만 한 번씩 구체적으로 안내한다.
3. 사용자 홈 전체·다른 사람 자료를 수집하지 않는다. 기존 ~/.codex 설정·인증·기억을 복제하지 않는다.
4. 기본 경로는 ~/AI Desk. Git 저장소를 만들지 않는다. 개인정보는 설치 키트에 쓰지 않는다.
5. scripts/setup.mjs는 미리보기 후 --apply. 기존 파일 충돌을 확인하고 유효한 내용은 보존한다.
   임의 덮어쓰기·삭제·force 플래그로 우회하지 않는다. 이 Mac의 현재 사용자 설정을 읽어 병합한다.
6. 새 세션 기본값 gpt-5.6-sol / medium + Full Access (danger-full-access / never), 전환 후보 gpt-6-astra / low.
   선택기에 없으면 유사 모델로 조용히 대체하지 말고 사용 가능 여부를 확인한다.
7. 공식 설치원·이미 설치된 연결 우선. 현재 컴퓨터 전용 경로·토큰·회사 자료를 배포물에 넣지 않는다.
8. 브라우저는 기존 Chrome 창의 자기 전용 새 그룹만. 새 창·별도 프로필·--direct로 우회 금지.
   승인 대기와 연결 실패를 구분하고 동일 팝업을 반복 생성하지 않는다.
9. 소유자가 실제로 사용할 수 있도록 한국어로 이끌고, 매뉴얼을 계정 종류와 선택 영역에 맞춰
   개인화한다. 사용자에게 모든 문서를 읽거나 수십 항목을 결정하라고 하지 않는다.
10. 매뉴얼 복사·도구 연결은 중간 단계다. SETUP.md 인수 항목을 실제 검증하고
    Memory/setup-status.md에 완료/미검증/차단을 구분해 기록한다.
11. 현재 세션에서 UI 권한을 받을 수 없으면 정확한 사용자 동작과 재개 문장을 남기되,
    독립적으로 가능한 폴더·매뉴얼·설정 검증은 계속한다. 설치 완료라고 과장하지 않는다.

키트 유지보수: node --test tests/*.test.mjs 및 node scripts/check-links.mjs 실행.
시크릿·사용자 이름·고객 자료는 커밋 금지. 임시 테스트는 OS 임시 폴더에서만.
